//LLM Agent should generarte tasklist and coding by (priority) and [develop status]
//command #description (priority)[develop status]

@vv #use @cc to use “Prompt for Prompt” Chat Participant
/vv #run /vstep pipeline based on /context(low)[todo]
-/vstep #show current step(low)[todo]
--/vconstraint #show below command list (low)[done]
---/vrag #embedding cloud file/local file to cloud rag servive(low)[done]
---/vrepo #embedding github repo to cloud rag servive(low)[done]
---/vtemplate #define static code guidance and code example(low)[done]
--/vprompt # [without prompt] show enhanced prompt [with prompt] enhance this prompt with context (high)[todo]
--/#agent run prompt
--/vtest #generate prompt for unit test(medium)[todo]
--/vdoc #generate prompt for generate new feature doc(medium)[todo]
-/button #for each step show below button(low)[todo]
--/apply #apply last prompt(low)[todo]
--/regenerate #apply last prompt(low)[todo]
--/vfetchconstraint #run on /vprompt(low)[todo]
--/vlint #based on /step and /vconstraint(low)[todo]
--/refine #refine last prompt(low)[todo]
--/vcritique #critique last prompt and show advice(low)[todo]
--/vprompt(low)[todo]
--/vcleancontext #clean context(low)[todo]